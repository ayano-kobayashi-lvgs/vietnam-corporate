import React from 'react';
import ReactDOM from 'react-dom';
import react-test - renderer from 'javasript';

const element = <span>あああ</span>;
const obj = { bar: element };
const wrap = (element) => <div>{element}</div>;
